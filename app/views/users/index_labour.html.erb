<div class="demo-content1">
<table>
<tr><td>
<h4>Labour</h4></td>
<td align="right">
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<div class="btn-group">
<% if not factory? %>
<div class="butt">
<%= link_to "Add", users_add_labour_path,:class=>"btn btn-success" %>
</div>
<%end%>
</div>
</td>
</tr>
</table>
<br><br>
<div class="fott">
<div class="digg_pagination" align="right">
    <%= will_paginate @labour, :container => false %>
  </div>
</div>
<br><br><br>
<table align="center">
<!--%= will_paginate @labour, renderer: BootstrapPagination::Rails %-->
<tr>
<h4 align="center">List Of Labours</h4></tr>
<tr><td>
<table border = "1" style="box-shadow:3px 3px 5px #000000;">
<tr><th>Order No</th>
<th>Issue Date</th>
<th>Issue Slip No</th>
<th>Shift</th>
<%if admin?%>
<th>Created at</th>
<th>Updated at</th>
<%end%>
</tr>
<ul id = "Labours" >
<% @labour.each do |i| %>
<tr>
<td><div class="fot"><%= i.order_no%></div></td>
<td><div class="fot"><%= i.dated%></div></td>
<td><div class="fot"><%= i.issue_slip_no%></div></td>
<td><div class="fot"><%= i.shift%></div></td>

<% if factory? %>
<td><%= link_to "Show", {:action => 'show_labour', :id => i.id },:class=>"btn btn-warning" %></td>
<%end%>
<% if admin? %>
<td><div class="fot"><%= i.created_at.strftime("%Y-%m-%d")%></div></td>
<td><div class="fot"><%= i.updated_at.strftime("%Y-%m-%d")%></div></td>
<td><%= link_to "Show", {:action => 'show_labour', :id => i.id },:class=>"btn btn-warning" %></td>
<td><%= link_to "Edit", {:action => 'edit_labour', :id => i.id },:class=>"btn btn-primary" %></td>
<td><%= link_to "Delete", {:action => 'delete_labour', :id => i.id },:class=>"btn btn-danger",:onclick => 'alert("Are you Sure!")' %></td>
<%end%>

<% if operator?%>
<%unless i.updated_at <(Time.now- 1.day)%>
<td><%= link_to "Edit", {:action => 'edit_labour', :id => i.id },:class=>"btn btn-primary" %></td>
<td><%= link_to "Delete", {:action => 'delete_labour', :id => i.id },:class=>"btn btn-danger",:onclick=>"if (confirm('Are you sure!!!')) commentDelete(1); return false" %></td>
<% end%>
<%end%>

</tr>
<%end%>
</ul>
</table>
</td></tr>

<!--%= will_paginate @labour, renderer: BootstrapPagination::Rails %-->
</table>
<br><br><br>
<div class="fott">
<div class="digg_pagination">
    <%= will_paginate @labour, :container => false %>
  </div>
</div>
</div>
